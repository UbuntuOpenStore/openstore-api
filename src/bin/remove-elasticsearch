#!/usr/bin/env node

'use strict';

const PackageSearch = require('../db/package/search');

PackageSearch.removeIndex().then(() => {
    console.log('done');
    process.exit(0);
}).catch((err) => {
    console.log(err);
    process.exit(1);
});
